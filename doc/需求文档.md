# 需求1

对话侧边栏，用户在对话界面，可以语音发送或者文字发送消息，配置多模态模型支持解析图片消息，这一步用工作流来处理。系统收到消息后自动解析其中的日程信息，并执行日历或者闹钟的CRUD。消息仅支持单轮次，即每次调用不发送历史，确保对话记录原子化。倒计时3s后自动放弃执行解析的CRUD操作，3s时长可配置，3s期间用户可点击确认真实执行或者放弃。放弃或者时间过期后自动放弃的，用户可以在历史记录进行再次执行
